#include<stdio.h>


int main()

{
    int i, n, X = 1, a = 0, x, p, h, b = 1,q;
    long long Sum;
    scanf("%d%d",&x,&p);

    for(h = 1; h <= n; h += 1 )

    b = x*b;

    n = 2*p;f

    for(i = 1; i <= n; i++)
    X *= i;

   for(a = 0; a <= n; a+= 2)
    Sum = (b/X);
 printf("%d",X);

    return 0;
}
