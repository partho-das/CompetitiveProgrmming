#include <stdio.h>
#include <string.h>

int main()
{
    unsigned int n, i, j, k;
    scanf("%u", &n);
    int jal[n];
    char name[20];
    for(i = 0; i < n; i++) scanf("%s", name);
    for(i = 0; i < n; i++)
    {
        for(j = 0; j < )
    }
    return 0;
}
